const estado: string = "São Paulo";
console.log(`Estado: ${estado}`);

// O código comentado abaixo gera o erro: Cannot assign to 'estado' because it is a constant.ts(2588)
//estado = "Rio de Janeiro";